name: Timecard Service – CI

on:
  push:
    paths:
      - "services/timecard-service/**"

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Build Docker image (CI validation only)
        run: |
          docker build \
            -t timecard-service-ci:${{ github.sha }} \
            services/timecard-service